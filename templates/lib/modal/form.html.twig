{% extends "@PonchoAdmin/lib/modal/default.html.twig" %}

{% block modal_wrapper %}
    {{ form_start(form, {attr: {'action' : null, 'data-xhr' : action | default(app.request.uri) }}) }}
        {{ parent() }}
    {{ form_end(form) }}
{% endblock %}

{% block modal_content %}
    {{ poncho_form_theme(form, form_layout | default(null)) }}
    {{ form_errors(form) }}
    {{ form_rest(form) }}
{% endblock %}

{% block modal_footer %}
    <div class="modal-footer">
        <button type="button" class="btn btn-light ms-auto" data-bs-dismiss="modal">
            <i class="mdi mdi-close me-1"></i> {{ 'action.close' | trans({}, 'PonchoAdmin') }}
        </button>

        <button type="submit" class="btn btn-primary">
            <i class="mdi mdi-check me-1"></i> {{ 'action.save' | trans({}, 'PonchoAdmin') }}
        </button>
    </div>
{% endblock %}
