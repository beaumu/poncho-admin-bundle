<div class="text-center mb-4">
    {% if poncho_admin.appLogo() %}
        <img src="{{ asset(poncho_admin.appLogo()) }}" height="36" alt="{{ poncho_admin.appName() }}">
    {% else %}
        <h1>{{ poncho_admin.appName() }}</h1>
    {% endif %}
</div>