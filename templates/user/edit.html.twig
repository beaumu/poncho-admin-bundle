{% extends "@PonchoAdmin/edit_modal.html.twig" %}

{% block modal_title %}
    <h4 class="modal-title">{{ entity.id
        ? ( 'action.edit_user' | trans({}, 'PonchoAdmin'))
        : ( 'action.add_user' | trans({}, 'PonchoAdmin')) }}</h4>
{% endblock %}